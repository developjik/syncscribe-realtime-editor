# SyncScribe — Real-time Collaborative Editor

프론트엔드 이직용 서브 프로젝트로 만든 **실시간 협업 에디터**입니다.  
핵심 목표는 아래 3가지를 실제 코드로 증명하는 것입니다.

- CRDT 기반 동시 편집 처리
- 협업 UX(커서/방 단위 세션)
- 실무형 제품 구조(로그인, 문서목록, 배포 준비)

## Stack

- React + TypeScript + Vite
- Tiptap Editor
- Yjs (CRDT)
- y-webrtc (P2P 실시간 동기화)
- y-indexeddb (오프라인 복구)
- LocalStorage 기반 간단 인증/문서 메타데이터 저장

## Implemented (Step 1 + Step 2)

### Step 1

- 실시간 공동 편집 (같은 Room ID 접속)
- 사용자별 협업 커서 표시
- IndexedDB 기반 오프라인 복구

### Step 2

- 로그인 화면 + 사용자 프로필 로컬 저장
- 문서 목록 사이드바 (생성/선택/삭제)
- 문서별 room 분리 협업
- 로그아웃/세션 상태 유지

## Architecture (요약)

- `문서 본문`: Yjs Doc + WebRTC provider로 동기화
- `문서 메타`: 로컬 스토리지(`title`, `updatedAt`, `id`) 관리
- `사용자 상태`: 로컬 스토리지(`name`, `color`) 관리
- `협업 세션`: 선택된 문서의 `id`를 room id로 사용

## Quick Start

```bash
npm install
npm run dev
```

동작 확인:

1. 브라우저 탭 2개를 연다.
2. 같은 문서를 선택한다.
3. 텍스트 입력 시 실시간 동기화되는지 확인한다.

## Build

```bash
npm run build
npm run preview
```

## Deploy (Vercel)

이미 `vercel.json`이 포함되어 있어 바로 배포 가능합니다.

```bash
npm i -g vercel
vercel
```

## This Week Updates (2026-03-16 ~ 2026-03-22)

### 1) 마지막 선택 문서 자동 복원

- 앱 재방문 시 마지막으로 작업하던 문서를 자동 선택하도록 복원 로직 추가
- 진입 직후 문서를 다시 찾는 시간을 줄여 협업 재개 속도 개선

### 2) 문서 목록 검색 UX 추가

- 문서 제목 기준 검색 필드를 도입해 문서 수가 많아져도 빠르게 탐색 가능
- 문서 전환 탐색 비용을 낮춰 "찾기-진입" 흐름의 마찰 감소

### 3) Room ID로 문서 참여 UX 추가

- Room ID를 직접 입력해 문서에 참여할 수 있는 진입 경로 제공
- 링크 공유가 어려운 상황에서도 협업 세션 합류가 가능해져 접근성 향상

### 4) 검색 단축키 + 빠른 초기화 UX

- 검색 포커스 단축키와 초기화 인터랙션을 추가해 키보드 중심 워크플로우 강화
- 반복 탐색 작업의 조작 횟수를 줄여 실사용 반응성 개선

## Interview Highlights (문제-해결-성과)

1. **문제:** 재접속할 때 매번 문서를 다시 찾아야 해 협업 재진입이 느렸음  
   **해결:** 마지막 선택 문서를 저장/복원하는 로직을 추가  
   **성과:** 재방문 즉시 작업 이어쓰기가 가능해져 진입 마찰 감소

2. **문제:** 문서가 늘어나며 목록 탐색 시간이 빠르게 증가함  
   **해결:** 제목 기반 실시간 검색과 초기화 UX를 구현  
   **성과:** 원하는 문서까지 도달하는 클릭/스크롤 비용을 크게 절감

3. **문제:** 기존에는 동일 링크 공유 중심이라 협업 세션 합류 경로가 제한적이었음  
   **해결:** Room ID 직접 입력으로 문서 참여 플로우를 추가  
   **성과:** 공유 방식이 달라도 세션 합류가 가능해져 협업 시작 실패율 감소

4. **문제:** 문서 검색을 마우스 중심으로만 조작해 반복 작업 시 피로도가 높았음  
   **해결:** 검색 포커스 단축키와 빠른 초기화 동작을 추가  
   **성과:** 파워유저 기준 작업 속도 향상 및 UX 완성도 상승

## Portfolio Description Draft

- **한 줄 소개:** "SyncScribe는 CRDT(Yjs) 기반 실시간 협업 에디터로, 이번 주에는 재진입 복원·검색·Room 참여 UX를 고도화해 협업 시작/탐색의 마찰을 줄였습니다."
- **면접용 3문장 버전:**
  1. "실시간 동기화 자체를 넘어서, 사용자가 실제로 자주 겪는 ‘다시 들어오기/문서 찾기/세션 합류’ 구간을 핵심 UX 문제로 정의했습니다."
  2. "마지막 문서 자동 복원, 문서 목록 검색+단축키, Room ID 참여 플로우를 한 주 동안 연속 개선해 협업 진입 속도와 탐색 효율을 함께 올렸습니다."
  3. "결과적으로 기술 데모가 아닌 ‘실사용에서 덜 막히는 협업 에디터’를 프론트엔드 설계/구현으로 증명한 프로젝트라고 설명할 수 있습니다."

## Next Steps (Portfolio 강화)

- 인증 고도화 (Supabase/Auth.js)
- 문서 목록 서버 저장 + 권한 모델
- 변경 이력/버전 롤백
- Playwright E2E + 성능 측정 리포트
- 아키텍처 다이어그램 + 트러블슈팅 기록

## Weekly Roadmap

- 2026-03-23 ~ 2026-03-29: `docs/ROADMAP_2026-03-23_to_2026-03-29.md`
- 2026-03-16 ~ 2026-03-22: `docs/ROADMAP_2026-03-16_to_2026-03-22.md`
- 2026-03-09 ~ 2026-03-15: `docs/ROADMAP_2026-03-09_to_2026-03-15.md`
